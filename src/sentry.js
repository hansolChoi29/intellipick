"use strict";
// import * as Sentry from "@sentry/react";
// import { BrowserTracing } from "@sentry/tracing"; // @sentry/tracing에서 BrowserTracing을 가져옵니다.
// Sentry.init({
//   dsn: "https://your-public-dsn@sentry.io/your-project-id", // 프로젝트에서 받은 DSN 입력
//   integrations: [new BrowserTracing()], // BrowserTracing 사용
//   tracesSampleRate: 1.0, // 샘플링 비율 설정
// });
